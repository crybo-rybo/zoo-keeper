name: Build and Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: build-and-test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format-18

      - name: Check formatting
        run: |
          mapfile -t files < <(git ls-files '*.hpp' '*.cpp' '*.h' '*.c' | grep -v '^extern/')
          if [ ${#files[@]} -eq 0 ]; then
            exit 0
          fi
          clang-format-18 --dry-run --Werror "${files[@]}"

  static-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install clang-tidy
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 clang-tidy-18 libc++-18-dev libc++abi-18-dev
          echo "CC=clang-18" >> $GITHUB_ENV
          echo "CXX=clang++-18" >> $GITHUB_ENV
          echo "CXXFLAGS=-stdlib=libc++" >> $GITHUB_ENV
          echo "LDFLAGS=-stdlib=libc++" >> $GITHUB_ENV

      - name: Configure CMake
        run: cmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DZOO_BUILD_TESTS=ON -DZOO_BUILD_EXAMPLES=ON -DZOO_BUILD_BENCHMARKS=ON

      - name: Run clang-tidy
        run: |
          mapfile -t files < <(git ls-files '*.cpp' | grep -v '^extern/' | grep -v '^tests/integration/')
          if [ ${#files[@]} -eq 0 ]; then
            exit 0
          fi
          clang-tidy-18 -p build --quiet \
            --header-filter='^(include/zoo|src|tests|examples)/' \
            "${files[@]}"

  build-and-test:
    name: build-and-test (${{ matrix.name }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-gcc-unit
            os: ubuntu-latest
            compiler: gcc
            cmake_flags: "-DZOO_BUILD_TESTS=ON"
          - name: linux-clang-full
            os: ubuntu-latest
            compiler: clang
            cmake_flags: "-DZOO_BUILD_TESTS=ON -DZOO_BUILD_INTEGRATION_TESTS=ON -DZOO_BUILD_EXAMPLES=ON -DZOO_BUILD_BENCHMARKS=ON"
          - name: macos-clang-metal
            os: macos-latest
            compiler: clang
            cmake_flags: "-DZOO_BUILD_TESTS=ON -DZOO_ENABLE_METAL=ON"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Clang (Ubuntu)
        if: matrix.os == 'ubuntu-latest' && matrix.compiler == 'clang'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 libc++-18-dev libc++abi-18-dev
          echo "CC=clang-18" >> $GITHUB_ENV
          echo "CXX=clang++-18" >> $GITHUB_ENV
          echo "CXXFLAGS=-stdlib=libc++" >> $GITHUB_ENV
          echo "LDFLAGS=-stdlib=libc++" >> $GITHUB_ENV

      - name: Build
        run: scripts/build.sh ${{ matrix.cmake_flags }}

      - name: Run tests
        run: scripts/test.sh

  package-consumer-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install package dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nlohmann-json3-dev

      - name: Configure producer build
        run: cmake -B build

      - name: Build producer library
        run: cmake --build build -j$(nproc)

      - name: Smoke test build-tree package consumer
        run: |
          cmake -S tests/packaging/cmake_consumer -B build/package-buildtree \
            -DCMAKE_PREFIX_PATH="${GITHUB_WORKSPACE}/build"
          cmake --build build/package-buildtree -j$(nproc)

      - name: Smoke test FetchContent consumer
        run: |
          cmake -S tests/packaging/fetchcontent_consumer -B build/package-fetchcontent \
            -DZOO_KEEPER_SOURCE_DIR="${GITHUB_WORKSPACE}"
          cmake --build build/package-fetchcontent -j$(nproc)

      - name: Install package to temp prefix
        run: cmake --install build --prefix "${RUNNER_TEMP}/zoo-install"

      - name: Smoke test install-tree package consumer
        run: |
          cmake -S tests/packaging/cmake_consumer -B build/package-installtree \
            -DCMAKE_PREFIX_PATH="${RUNNER_TEMP}/zoo-install"
          cmake --build build/package-installtree -j$(nproc)

  sanitizers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Clang
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18
          echo "CC=clang-18" >> $GITHUB_ENV
          echo "CXX=clang++-18" >> $GITHUB_ENV

      - name: Build with sanitizers
        run: scripts/rebuild.sh -DZOO_ENABLE_SANITIZERS=ON -DZOO_BUILD_TESTS=ON

      - name: Run tests under sanitizers
        run: scripts/test.sh

  coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install coverage tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 llvm-18 lcov
          echo "CC=clang-18" >> $GITHUB_ENV
          echo "CXX=clang++-18" >> $GITHUB_ENV
          cat > "${RUNNER_TEMP}/llvm-gcov-18" <<'EOF'
          #!/usr/bin/env bash
          exec llvm-cov-18 gcov "$@"
          EOF
          chmod +x "${RUNNER_TEMP}/llvm-gcov-18"
          echo "GCOV_TOOL=${RUNNER_TEMP}/llvm-gcov-18" >> $GITHUB_ENV

      - name: Build with coverage
        run: scripts/rebuild.sh -DZOO_ENABLE_COVERAGE=ON -DZOO_BUILD_TESTS=ON

      - name: Run tests
        run: scripts/test.sh

      - name: Capture coverage
        run: |
          lcov --gcov-tool "${GCOV_TOOL}" --capture --directory build --output-file coverage.info \
            --ignore-errors mismatch,inconsistent
          lcov --remove coverage.info '/usr/*' '*/extern/*' '*/build/_deps/*' '*/tests/*' \
            --output-file coverage.info --ignore-errors unused

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.info

      - name: Upload to Codecov
        if: env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v4
        with:
          files: coverage.info
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
