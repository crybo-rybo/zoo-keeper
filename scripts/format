#!/usr/bin/env bash
# Run clang-format on all C++ source and header files.
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
find src include tests examples benchmarks -type f \( -name '*.cpp' -o -name '*.hpp' \) | xargs clang-format -i
echo "Formatting complete."
