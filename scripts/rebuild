#!/usr/bin/env bash
# Clean all build artifacts and rebuild from scratch.
# Usage: scripts/rebuild [extra cmake flags...]
#   e.g. scripts/rebuild -DZOO_BUILD_EXAMPLES=ON
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
rm -rf build
scripts/build "$@"
